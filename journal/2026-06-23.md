# 2026-06-23

- **Code Editor improvements**: its own undo/redo stack while typing (instead of relying on the browser's), a dot indicator on files with uncommitted changes, a round play button for opening the Preview window, and the tab now shows just the bold filename instead of the full path.
- **Media panel fixes**: the toolbar now stays sticky at the top while the grid scrolls, and fixed a bug where deselecting an image with the sidebar off would resize the whole window.
- Daily Notes: nicer spacing, and the page now auto-refreshes across midnight without needing a reload.
