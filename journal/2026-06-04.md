# 2026-06-04

- Built out the photo editor: crop tool, tonal adjustments (brightness/contrast etc. via GPU shaders), and the ability to copy/paste edits across photos (including in batches, with thumbnails reflecting the edits).
- Added "Export for web" for single photos and batches, and consolidated it with regular export.
- Added background removal, iterating through a few approaches before landing on Apple's native Vision framework (faster/better than the earlier ML-model-based versions).
- Native quality-of-life: live-refresh when files change on disk, QuickLook-style thumbnails, and pasting images from the clipboard (Cmd+V) directly into a project.
- Sped up media library loading and fixed duplicate-thumbnail bugs.
- Notes/tables: editing styled consistently, tables start with a default 2x2 grid, added column totals, drag handles for rows/columns, and pasting from spreadsheets (TSV) now works.
