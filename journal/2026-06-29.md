# 2026-06-29

- **New Tasks feature**: calendar-driven meeting notifications that pop a persistent card at a configurable lead time. Handles transit-aware lead times for in-person events (recomputed when the buffer changes), a Join link that opens directly, and a Claude skill (`studio-tasks`) so Claude can author and edit tasks by name. Cards animate in on add/edit and flip in place to reveal the edit form.
- Task card polish: each task is now an elevated card, the date/time field is split into separate inputs, added a card-reveal animation, and the toolbar uses icon-only tune/refresh buttons with a round add button.
- Fixed a Daily Briefing image fetch bug (bundled with the 6-28 journal commit).
