# 2026-06-26

- **Claude window gets the custom chrome treatment**: project-color tinted window matching the rest of the custom chrome system, with the context-usage bar shown more selectively and select buttons moved to the bottom for easier reach.
- General polish pass: consolidated duplicate tab styles, fixed a dropdown button bug, switched to a larger border radius across the kit, and a round of layout/CSS fine-tuning.
