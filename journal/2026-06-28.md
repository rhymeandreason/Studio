# 2026-06-28

- **Code Editor now opens per-project**: each project gets its own Code Editor window and session instead of sharing one, with file-opens scoped per-window so opening a file from one project doesn't hijack another's window.
- **Claude window polish**: the in-Studio Claude window got the custom project-color chrome treatment (matching Code Editor/main app), and can now answer its own clarifying questions with clickable option buttons instead of typed replies.
- **Daily Briefing improvements**: switched to a real two-column layout with a Wikimedia "picture of the day," pulls actual weather data instead of having the LLM guess, and now uses the minimal window style.
- Smaller fixes: Code Editor's missing color CSS, plus a round of general CSS cleanup.
