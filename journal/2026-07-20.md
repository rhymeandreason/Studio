# 2026-07-20

- Slides got new layout options — grid, stats, and steps layouts share a common item structure, and the Image + Text layout can now show two stacked images instead of just one.
- File Directory now refreshes automatically when files change on disk, including for repo and folder paths, so it stays in sync without needing a manual reload.
- Git panel and Git Pulse now refresh automatically after commits/undos and when the window regains focus, so you're not looking at stale status.
- Media polish: pressing spacebar on a selected image opens Quickview, and doing the same on a video now opens it in the default player instead of Quickview (which was crashing on some video files).
</content>
