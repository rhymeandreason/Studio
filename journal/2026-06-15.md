# 2026-06-15

- **Design System**: Built out a proper design system — shared UI components (buttons, toggles, tabs, dropdowns, swatches, preview cards) are now in a central kit stylesheet, and all panels use the canonical class names. Renamed the "Kit Gallery" tool to "Design System" and rebuilt the page with a clean two-column reference layout.
- **Brand Explorer**: Major polish pass — square color swatch grid with an inline color picker, correct font weight rendering in preview cards, and a Save button that overwrites the active file (vs. Duplicate for creating new ones). Expanded the Google Fonts list to ~144 options.
- **Artifacts panel**: Cards are now selectable — single-click to select, Delete key to remove.
