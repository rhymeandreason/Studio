# 2026-06-18

- **Code Editor: Markdown support** — the editor now opens Markdown files with a toggle between raw edit and rendered preview, a section browser for jumping between headings, clickable links that open in the Preview window, a linked-files list, nav history, and preserved scroll position across mode switches. Also added a find feature (a little rough still).

- **Git Pulse tool** — new tool that draws a dot graph of commits from this week across all branches, polished to a minimal window style with no path input.

- **Media editor improvements** — added a sidebar toggle button, a "None" crop button, moved Reset All into the sidebar dropdown, fixed selection staying on toolbar clicks, fixed Cmd+C copying the image when the sidebar is closed, and prevented the sidebar from reopening on tab switch when it was toggled off. Also added image attachment to text notes via paste.

- **Window Size tool** — new utility for snapping/resizing windows, with docs covering the `winbounds` helper and the always-on-top pattern.

- Cleaned up the repo for publishing and refreshed several docs (media, notes, workspace, scheduled tasks, git pulse).

- **Switched to Futura PT** across the design system, then regretted this late night decision.
