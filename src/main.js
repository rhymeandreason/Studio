// Studio frontend shell.
// M1: the window is just an empty "no project active" surface. The tray (Rust
// side) owns the menu-bar icon and the hardcoded "Hello" entry. Real project
// activation, the header, and the panels arrive in M2+.

window.addEventListener("DOMContentLoaded", () => {
  // Nothing dynamic yet. Placeholder for the activation listener wired in M2.
});
