[package]
name = "studio-claude-core"
version = "0.1.0"
edition = "2021"
description = "Claude Code integration shared by Studio and the standalone companion"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ureq = { version = "2", features = ["json"] }
