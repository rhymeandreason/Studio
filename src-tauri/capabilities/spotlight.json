{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "spotlight",
  "description": "Capability for the Spotlight-style quick launcher and Mode switcher",
  "windows": ["spotlight", "mode-switcher"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-hide",
    "core:window:allow-set-size",
    "core:window:allow-center"
  ]
}
