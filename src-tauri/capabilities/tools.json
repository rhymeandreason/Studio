{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "tools",
  "description": "Capability for standalone Tools windows (~/Projects/Tools/*.html)",
  "windows": ["tool-*"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-set-size",
    "dialog:default"
  ]
}
