{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "claude",
  "description": "Capability for the Claude companion chat window",
  "windows": ["claude"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-set-title",
    "dialog:default"
  ]
}
